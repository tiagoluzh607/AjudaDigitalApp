<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ftec.ajudadigital">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <activity
            android:name=".LoginActivity"
            android:label="Ajuda Digital">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CampanhaActivity"
            android:label="Campanhas"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity android:name=".FormVoluntarioActivity" />
        <activity
            android:name=".ParticiparCampanhaActivity"
            android:label="Minhas Participações" />
        <activity android:name=".FormDoacaoActivity" />
        <activity android:name=".AfinidadeActivity"></activity>
    </application>

</manifest>